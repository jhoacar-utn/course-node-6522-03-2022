git pull origin lessons 
Traer las clases con conflictos, pero no tener en cuenta los conflictos.

Pasar todas carpetas a mi carpeta
git merge --abort, para volver al estado normal.

Luego de terminar lo anterior, subo mis cambios con los siguientes comandos:
git add .
git commit -m ...
git push